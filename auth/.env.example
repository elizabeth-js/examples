# Required: a long random string used to sign session cookies.
# Generate one with: bun -e "console.log(crypto.getRandomValues(new Uint8Array(32)).reduce((acc, b) => acc + b.toString(16).padStart(2, '0'), ''))"
SESSION_SECRET=replace-me-with-a-real-secret
